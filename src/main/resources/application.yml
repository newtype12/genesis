server:
  port: 8085

spring:
  profiles:
    include: h2
  jpa:
    show-sql: false
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop

  activemq:
    in-memory: true
    packages:
      trust-all: true


